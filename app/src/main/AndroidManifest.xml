<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:label="VisioType"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.DarkActionBar">

        <activity android:name=".IntroductionActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".AgeQuestionActivity" />
        <activity android:name=".GenderQuestionActivity" />
        <activity android:name=".EducationQuestionActivity" />
        <activity android:name=".WelcomeActivity" />
        <activity android:name=".QuizQuestionActivity" />
        <activity android:name=".PostGameActivity" />
        <activity android:name=".PostQuestion1Activity" />
        <activity android:name=".PostQuestion2Activity" />
        <activity android:name=".ThankYouActivity" />
        <activity android:name=".ResultActivity" />

    </application>

</manifest>